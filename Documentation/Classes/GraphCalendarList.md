# GraphCalendarList Class

## Overview

Pageable list of Outlook calendars returned by a Graph API query.
The `calendars` getter returns the list as a `Collection` of plain objects.

## Properties

A `GraphCalendarList` object exposes the following properties:

| Property | Type | Description |
|---|---|---|
| calendars | Collection | (read-only) The current page of calendar objects |

## See also

* [Office365Calendar](./Office365Calendar.md)
